/* snmp.h - a Windows subsystem header PreRTS.h names.  The declarations this tree actually uses are
** in Platform/Win32Compat.h; the subsystem itself has no macOS counterpart and the files that
** would drive it are out of this build. */
#ifndef WIN32COMPAT_SHIM_SNMP_H
#define WIN32COMPAT_SHIM_SNMP_H
#include "Platform/Win32Compat.h"
#endif
